Repository for testing communication between a Node.js WebSocket server and a C++ client.

Download repository and install dependencies.

```npm install```

Run WebSocket server using:


`node server.js`